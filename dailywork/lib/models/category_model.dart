class CategoryModel {
  final String id;
  final String name;
  final String iconName; // maps to a Material icon name

  const CategoryModel({
    required this.id,
    required this.name,
    required this.iconName,
  });
}
